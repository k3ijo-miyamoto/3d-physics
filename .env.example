# Copy this file to .env.local and set your API key.
# .env.local is git-ignored and will NOT be committed.
#
# Note: Vite embeds VITE_* variables into the client bundle.
# This setup is intended for local development only.
# For production, proxy API calls through a backend server.

VITE_ANTHROPIC_API_KEY=sk-ant-...
